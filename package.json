{
  "name": "forensik-vision",
  "version": "1.0.0",
  "private": true,
  "description": "Forensische Bildanalyse mit Live-Objekterkennung",
  "scripts": {
    "sync": "cap sync",
    "android": "cap sync android && cd android && ./gradlew assembleDebug",
    "serve": "python3 -m http.server 8080 --directory www"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
